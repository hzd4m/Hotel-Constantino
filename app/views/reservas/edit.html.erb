<% content_for :title, "Editar reserva" %>

<div class="page-header">
	<div class="stack-sm">
		<span class="page-eyebrow"><i class="bi bi-pencil"></i> Ajustes</span>
		<h1 class="page-header__title">Editar reserva</h1>
		<p class="page-header__subtitle">Atualize datas, status e valores mantendo a equipe sincronizada.</p>
	</div>
	<div class="page-actions">
		<%= link_to 'Voltar', reservas_path, class: "btn btn-outline-secondary" %>
	</div>
</div>

<div class="surface-card surface-card--compact stack-md">
	<%= render 'form', reserva: @reserva %>
</div>
